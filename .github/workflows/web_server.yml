name: GitHub Actions On Push 
on: workflow_dispatch

jobs:
  clone-website-and-run-podman:
    runs-on: ubuntu-latest


    steps:
      - name: Git Clone Repo
        run: git clone https://github.com/Ryshai/ghactionsdemo.git
      - name: Check Website Files
        run: ls -lart